# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.py[cod]
.DS_Store

# Virtual environments
.env
.venv/
venv/

# ── Internal / session files ──────────────────────────────────────────────────
HANDOFF.md
download.log
memory/

# ── Data files (large, regenerated by run_cleaning.py) ───────────────────────
# download_amazon_large.py is committed — it's the large-dataset download script archive
data/category_cache/
data/train.parquet
data/test.parquet
data/train_ft.txt
data/test_ft.txt

# Small-dataset arrow files (~400 MB each) — too large for GitHub.
# Re-download with: docs/notebooks/download_datasets.ipynb
data/amazon_data-*.arrow

# Reference UNSPSC datasets are committed intentionally:
#   data/UNSPSCdataset.csv
#   data/UNSPSCtestDataSet.csv

# ── Trained model artifacts (regenerated by run_modeling.py) ─────────────────
models/
evaluation/final_report.json

# ── Binary ML artifacts ───────────────────────────────────────────────────────
*.arrow
*.parquet
*.bin
*.pkl
*.npy
